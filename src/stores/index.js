import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

const state = {
  player: {}
}

const getters = {
}

const actions = {

}

const mutations = {
}

export default new Vuex.Store({
  strict: true,
  state,
  getters,
  actions,
  mutations
})
